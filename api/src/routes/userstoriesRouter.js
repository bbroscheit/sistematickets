const userstoriesRouter = require("express").Router()

userstoriesRouter.get( '/userstories', async ( req, res ) =>{
    try {
        let 
    } catch (e) {
        console.log("error en ruta get userstories", e.message);
    }
})

module.exports = userstoriesRouter